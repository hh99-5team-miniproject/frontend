import React from "react";
import { useNavigate } from "react-router-dom";

const Detail = () => {
  const navigate = useNavigate();

  return <div>여기는 상세페이지입니다.....</div>;
};

export default Detail;
